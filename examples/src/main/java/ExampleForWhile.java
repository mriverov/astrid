import java.util.List;

/**
 * Created by marina.rivero on 3/13/18.
 */
public class ExampleForWhile {

    /**
     * devuelve true si la variable toBeFound esta en la lista elements
     * @param elements
     * @param toBeFound
     */
    public void found(List<String> elements, String toBeFound){
        boolean found = false;

        for(String currentElement: elements){

        }

    }

    /**
     * devuelve true si la variable toBeFound esta en la lista elements
     * @param elements
     * @param toBeFound
     */
    public void found2(List<String> elements, String toBeFound){
        boolean found = false;
        boolean condition = false;

        while(condition){

        }

    }

    /**
     * Devuelve dos listas, una con la primera mitad de elements y otra con al segunda.
     * Si elements es [1,2,3,4,5,6]
     * la funcion tiene que devolver [1,2,3] y [4,5,6]
     * Si elements es [1,2,3,4,5]
     * la funcion tiene que devolver [1,2,3] y [4,5]
     * @param elements
     */
    public void split(List<String> elements){

        for(String currentElement: elements){

        }

    }

    /**
     * Devuelve dos listas, una con la primera mitad de elements y otra con al segunda.
     * Si elements es [1,2,3,4,5,6]
     * la funcion tiene que devolver [1,2,3] y [4,5,6]
     * Si elements es [1,2,3,4,5]
     * la funcion tiene que devolver [1,2,3] y [4,5]
     * @param elements
     */
    public void split2(List<String> elements){
        boolean condition = false;

        while(condition){

        }

    }
}
